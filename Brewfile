# Restore with: brew bundle --file=Brewfile
tap "anomalyco/tap"

# GitHub CLI
brew "gh"
# Node version manager. Deliberately `n`, not nvm.
brew "n"
# Terminal-based AI coding agent
brew "anomalyco/tap/opencode"

# iTerm2 is the terminal these dotfiles are built around.
cask "iterm2", args: { adopt: true }
